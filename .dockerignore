# Keep Docker build context small (especially for `railway up`), so builds don't
# accidentally upload/copy local artifacts like `node_modules/`, `.next/`, or
# big indexing outputs.

# VCS / OS junk
.git
.DS_Store
**/.DS_Store

# Local artifacts / datasets
output/
tmp/
**/output/
**/tmp/
*.log

# Node / Next / bundlers
**/node_modules/
**/.next/
**/.turbo/
**/.cache/
**/.source/
**/dist/
**/build/

# Keep service sources included (Railway may build each service from repo root)
# but exclude their local build artifacts via the patterns above.
