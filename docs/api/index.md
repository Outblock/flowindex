# API Reference

Browse the FlowScan API using the sidebar. Endpoints are grouped by tag, and each operation
includes request/response details plus an interactive playground.

The OpenAPI schema is available at `/api/openapi.json`.
